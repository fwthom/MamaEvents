<h1>Participants#new</h1>
<p>Find me in app/views/participants/new.html.erb</p>

<div class="container">
<%= console %>
<%= @charity.first.name %>
<%= simple_form_for(@participant) do |f| %>
  <%= f.input :first_name, label: 'prénom' %>
  <%= f.input :last_name, label: 'nom' %>
  <%= f.input :email %>
  <%= f.submit %>
<% end %>


</div>
