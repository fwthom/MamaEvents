<div class="text-center"><h1>M'inscrire</h1></p>
<div class="container" style="max-width: 600px;">
  <%= simple_form_for([@event, @participant], local: true) do |f| %>
  <%= f.input :first_name, label: 'Prénom' %>
  <%= f.input :last_name, label: 'Nom' %>
  <%= f.input :email, label: 'Adresse e-mail' %>
  <%= f.input :tickets, label: "Ticket choisi", as: :select, collection: @event.tickets, label_method: :name, value_method: :id, input_html: { name: "ticket_id" } %>
  <%= f.submit "M'inscrire", class:"btn btn-primary" %>
<% end %>


<%= console %>


</div>
