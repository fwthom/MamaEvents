import { Controller } from "@hotwired/stimulus"

// Connects to data-controller="duplicate-options"
export default class extends Controller {
  connect() {
    console.log("duplicate init")
  }

  duplicate() {
    
  }
}
